stage:
  id: "09-testing"
  name: "Testing & E2E"

ai:
  models: [codex]
  primary: codex
  execution_mode: sandbox_playwright

dependencies: ["08-qa"]
checkpoint:
  required: false
timeout: 7200

git:
  commit_type: "test"
  commit_scope: "test"
