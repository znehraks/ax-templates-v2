# /restore

체크포인트에서 복구합니다.

## 사용법

```
/restore [checkpoint-id] [-f|--force] [--list]
```

## 인자

- `checkpoint-id` (선택): 복구할 체크포인트 ID (기본: 최신)
- `-f, --force`: 확인 없이 복구
- `--list`: 사용 가능한 체크포인트 목록 표시

## 동작

1. 체크포인트 존재 확인
2. 체크포인트 정보 표시
3. 복구 확인 요청
4. 파일 복구 실행
5. 진행 상태 업데이트

## 복구 프로세스

```
🔄 체크포인트 복구
══════════════════════════════════════════════════

체크포인트 정보:
  ID: cp_20240115_143000
  스테이지: 06-implementation
  시간: 2024-01-15T14:30:00
  메시지: 구현 50% 완료
  Git Ref: a1b2c3d4

복구될 파일:
  - stages/06-implementation/outputs
  - stages/06-implementation/HANDOFF.md
  - state/progress.json
  - ./my-app/src

⚠️  주의: 현재 파일이 체크포인트 버전으로 덮어씌워집니다.

체크포인트를 복구하시겠습니까? (y/N)
```

## 복구 결과

```
✓ 체크포인트 복구 완료

복구 결과:
  복구된 파일: 15개
    ✓ stages/06-implementation/outputs/source_code
    ✓ stages/06-implementation/HANDOFF.md
    ✓ state/progress.json
    ✓ ./my-app/src/...

Git 복구 (선택):
  git checkout a1b2c3d4

현재 상태 확인: /status
```

## Git 복구

체크포인트에 Git 참조가 포함된 경우 별도로 Git 복구를 수행할 수 있습니다:

```bash
git checkout <git-ref>
```

## 주의사항

- 복구 전 현재 작업을 저장하세요
- 복구하면 현재 파일이 덮어씌워집니다
- Git 작업 트리에 변경사항이 있으면 먼저 커밋하세요
