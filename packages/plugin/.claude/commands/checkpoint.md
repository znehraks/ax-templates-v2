# /checkpoint

체크포인트를 생성하거나 관리합니다.

## 사용법

```
/checkpoint [action] [checkpoint-id]
```

## 액션

- `create` (기본): 새 체크포인트 생성
- `list`: 체크포인트 목록 표시
- `show`: 특정 체크포인트 상세 정보

## 체크포인트 생성

```
/checkpoint
/checkpoint create -m "구현 50% 완료"
```

### 옵션
- `-m, --message <message>`: 체크포인트 설명
- `-s, --stage <stage-id>`: 스테이지 지정 (기본: 현재 스테이지)

### 포함되는 파일
- `stages/{stage-id}/outputs/` - 스테이지 산출물
- `stages/{stage-id}/HANDOFF.md` - 핸드오프 문서
- `state/progress.json` - 진행 상태
- `{project-root}/` - 소스 코드 (구현/리팩토링 스테이지)

## 체크포인트 목록

```
/checkpoint list
/checkpoint list -s 06-implementation
```

출력:
```
📋 전체 체크포인트 목록
══════════════════════════════════════════════════

cp_20240115_143000
  스테이지: 06-implementation
  시간: 2024-01-15T14:30:00
  메시지: 구현 50% 완료
  Git: a1b2c3d4

cp_20240115_120000
  스테이지: 05-task-management
  시간: 2024-01-15T12:00:00
  메시지: 태스크 분해 완료

──────────────────────────────────────────────────
총 2개 체크포인트
```

## 체크포인트 상세

```
/checkpoint show cp_20240115_143000
```

## 복구

체크포인트에서 복구하려면 `/restore` 명령어를 사용합니다:

```
/restore cp_20240115_143000
```
